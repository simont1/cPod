#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "library.h"
#include "song_node.h"

struct song_node * addSong(struct song_node * heck, struct song_node *insertee){
  insert_front(heck, insertee->name, insertee->artist);
  return heck;
}
